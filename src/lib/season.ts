/**
 * Single source of truth for the current season year.
 * Change this once each December to update all page titles,
 * meta descriptions, and H1s across the site.
 *
 * URLs are year-free (/calendar, /cheapest-races) so they
 * accumulate link equity. Only user-facing text uses this.
 */
export const CURRENT_SEASON = 2026;
